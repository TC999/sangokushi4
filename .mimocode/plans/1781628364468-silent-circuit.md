# Plan: 阶段三 — 集成测试与功能验证

## 当前状态

所有6个crate的模块骨架和核心逻辑已实现并编译通过（29个单元测试全部通过）。
根据架构文档 `specs/architecture_design.md` 的阶段规划，阶段二（逐模块实现）已完成。

## 下一步：阶段三 — 集成与验证

### 目标

创建全面的集成测试，验证核心游戏流程的正确性：
**开局 → 内政 → 军事 → 战斗 → 回合推进 → 结局**

### 具体任务

#### 1. 扩展 s4-core 的回合处理逻辑
文件: `crates/s4-core/src/round/mod.rs`
- 完善 `officer_loyalty_decay`（当前是空实现）
- 添加回合时间推进逻辑（年月日自动推进）
- 添加势力城市归属检查

#### 2. 扩展 s4-core 的战斗系统
文件: `crates/s4-core/src/battle/mod.rs`
- 添加战斗结果处理（胜败后部队状态变更、城市归属变更）
- 添加战力计算（基于武将属性+随机因子）
- 添加撤退判定

#### 3. 扩展 s4-core 的场景引擎
文件: `crates/s4-core/src/scene/mod.rs`
- 添加场景状态数据
- 添加战斗场景触发逻辑（检测相邻敌方单位）

#### 4. 创建 s4-core 集成测试
文件: `crates/s4-core/tests/integration_test.rs`（新建）
测试用例：
- `test_complete_game_flow` — 完整游戏流程：初始化→多回合运行→验证状态
- `test_round_processing_effects` — 回合处理效果验证（资源积累/消耗）
- `test_battle_outcome` — 战斗结果验证
- `test_ai_movement` — AI移动验证
- `test_city_development` — 城市开发验证
- `test_officer_loyalty` — 武将忠诚度验证

#### 5. 创建 s4-resource 集成测试
文件: `crates/s4-resource/tests/integration_test.rs`（新建）
测试用例：
- `test_lzss_roundtrip` — LZSS压缩→解压往返验证
- `test_gamma_coding_full_range` — 伽马编码全范围测试

### 关键修改文件

| 文件 | 修改内容 |
|------|---------|
| `crates/s4-core/src/round/mod.rs` | 完善忠诚度衰减、时间推进 |
| `crates/s4-core/src/battle/mod.rs` | 战斗结果处理、战力计算 |
| `crates/s4-core/src/scene/mod.rs` | 场景触发逻辑 |
| `crates/s4-core/tests/integration_test.rs` | 新建：核心游戏流程集成测试 |
| `crates/s4-resource/tests/integration_test.rs` | 新建：资源管道集成测试 |

### 验证方式

1. `cargo test --workspace` — 全部测试通过
2. 集成测试覆盖：初始化→回合→战斗→状态变更的完整链路
3. 与规格文档 /11, /12, /14 的行为描述对齐验证
